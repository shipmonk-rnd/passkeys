{
    "name": "jantvrdik/webauthn",
    "description": "Spec-compliant WebAuthn (passkeys) library for PHP with minimal dependencies",
    "license": "MIT",
    "require": {
        "php": ">=8.4",
        "ext-openssl": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^2.2",
        "shipmonk/coverage-guard": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "WebAuthnX\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "WebAuthnXTests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit tests",
        "phpstan": "phpstan analyse"
    }
}
